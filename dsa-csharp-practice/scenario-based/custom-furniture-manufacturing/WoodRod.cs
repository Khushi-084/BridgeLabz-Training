// Represents a wooden rod with a specific length.
class WoodRod{
    // Length of the wooden rod.
    public int Length { get; private set; }
    
    // Initializes the wooden rod with a given length.
    public WoodRod(int length){
        // Set the length of the rod
        Length = length;
    }
}
