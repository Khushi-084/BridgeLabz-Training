class Rod{
    // Length of the rod
    public int Length { get; private set; }
    
    // Constructor to initialize the rod with a given length
    public Rod(int length){
        Length = length;
    }
}
